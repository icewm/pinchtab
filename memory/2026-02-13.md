- 与用户排查 sing-box 1.12.21 配置兼容性；升级后 /etc/sing-box/config.json 出现弃用警告与启动失败（legacy tun address fields）。
- 给出迁移方案并生成新版配置：
  - DNS 服务器改为新格式（type https/udp，server/server_port/path，domain_resolver）。
  - 路由中 DNS 改为 action: hijack-dns。
  - 删除 outbounds 里的 dns-out。
  - tun 的 inet4_address -> address 数组。
  - 移除 dns.rules 中 outbound:any 项；去掉 domain_strategy，必要时在 outbound 上添加 domain_resolver。
- 已保存并发送文件：/home/icewm/.openclaw/workspace/config.new.json 与 config.fixed.json。
- 用户明确要求：以后凡是打开浏览器或进行浏览器搜索，一律使用 TOOLS.md 里“独立有头 Chrome（remote-debugging 18800，/tmp/openclaw-chrome）”的启动方法。
